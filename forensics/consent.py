"""Digital consent search — Discord messages × violations."""
from __future__ import annotations
from ..normalize.types import Observation, Message

def search(observations: list[Observation], messages: list[Message]) -> dict:
    """Search Discord for authorization near each violation."""
    # TODO: Extract from cross_reference_discord.py consent search
    return {}
