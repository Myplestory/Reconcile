"""Client-side source ingest — JS file loading and comparison."""
from __future__ import annotations
from ..config import PipelineConfig

def load(config: PipelineConfig) -> list[dict]:
    """Load JS source files, compute hashes, detect changes."""
    # TODO: Extract from source file analysis
    return []
